spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cine
    username: postgres
    password: Admin1236

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080